package com.lonewolf.wavvy.ui.theme

// Compose graphics
import androidx.compose.ui.graphics.Color

// Brand identity
val LyraCian = Color(0xFF00BFA5)
val LyraCianVariant = Color(0xFF00E5FF)

// Neutrals for Light/Dark modes
val WhiteSoft = Color(0xFFF8F9FA)
val DarkSoft = Color(0xFF0E0E16)
val GraySurface = Color(0xFF1A1A24)
val BlackAbsolute = Color(0xFF000000)

// Transparent utility colors
val WhiteAlpha15 = Color.White.copy(alpha = 0.15f)
val DarkAlpha08 = Color.Black.copy(alpha = 0.08f)
